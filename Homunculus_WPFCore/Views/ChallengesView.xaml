<UserControl x:Class="Homunculus_WPFCore.Views.ChallengesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Homunculus_WPFCore.Views"
             xmlns:vms="clr-namespace:Homunculus_WPFCore.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vms:ChallengesVM/>
    </UserControl.DataContext>
    <Grid>
        <ListView ItemsSource="{Binding Challenges}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Runs" DisplayMemberBinding="{Binding NumRuns}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
